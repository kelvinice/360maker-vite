import {Scene, SettingModel, Shortcut} from "../models/DataModel";

export const initialDataScene: Scene[] = [];
export const initialDataSetting: SettingModel = {
    initialScene: "",
    defaultMarkerSize: 32,
}
export const initialDataShortcut: Shortcut[] = [];


